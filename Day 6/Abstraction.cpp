#include<iostream>
using namespace std;

int main(){
    //IN THIS WE TALK ABOUT ABSTRACTION -- 
/*
    hiding all the unnessary details and showing only the important parts 

    for this we use access modifiers/specifiers -- 

    for private and sensitive information -- we use private modifiers

    for important parts we use -- public modifiers

    we want data into private but inherit then we use protected modifiers

    in data hiding we talk about -- only data hiding that is private modifiers

    but in the abstraction we talk about both hiding as well as showing important tasks

    --- we can also use Abstract classes for abstractions -- 
we just have to write abstract in front of class 

in normal classes we know class act as a blueprint of object how object look like  but in the abstract classes 

these classes make BLUEPRINT FOR OTHER CLASSES -- it is used to make for inheritance 
properties are -- 

abstract classes are used to provide a base class from which other classes can be derived 
-- they cannot be instantiated(means it is not an object) and used for the inherited purpose 
-- acts as a blueprint for the other classes
eg like 
we have a shape class 
abstract shape(){
 draw();
}  //it acts as a abstract class

class circle/square/other : public shape{
    draw(){
        cout<<"now we can make anytype of shape"<<endl        
}
};
*/
    return 0;
}